# Study Namespaces

`results/raw`, `results/summary`, `results/figures` は既存成果の baseline として扱います。

新しい研究テーマは `results/studies/<study_id>/...` 配下に分離し、baseline を直接更新しない運用を前提にします。

shifted DAG dynamic study の既定 namespace:

- `13_shifted_dag_dynamic/common_coupled_subset/`
